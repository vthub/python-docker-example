# Docker image example for Python script

Description of files:
* `Dockerfile` - definition of the docker image
* `index.py` - script to execute inside Docker
* `run.sh` - shortcut script to run docker container locally
